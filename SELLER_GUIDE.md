# Stock Screener Pro — 卖家使用手册（当前已就绪版）

> 你的密钥、公钥、激活模式都已配置好，并已自测通过。
> 你现在**只剩两件事**：把软件打包、每卖一份生成激活码。

---

## 你现在只需要做这两件事

### ① 打包成安装程序
```powershell
cd "C:\Users\ediso\OneDrive\Documents\harness\stock-screener"
build.bat                        # 生成 dist\StockScreenerPro.exe
```
然后打开 `installer.iss`，用 **Inno Setup** 点“编译”，得到发给买家的安装包（setup.exe）。

### ② 每卖一份，生成一个激活码（一码一机）
买家**装好并打开**软件后，会弹“激活”窗口，里面有一串**机器码**。让他**复制发给你**，然后你运行：
```powershell
cd "C:\Users\ediso\OneDrive\Documents\harness\stock-screener"
python seller_tools/create_license.py --name "买家名字" --order "单号" --machine "买家发你的机器码"
```
程序会打印一段**激活码**，整段复制发给买家。

> 因为这个码**已经绑定到他那台电脑**，别人拿去另一台电脑激活会提示 “not bound to this computer”——**有效防止买家转卖/共享激活码**。

---

## 买家怎么用（转述给他）
1. 双击安装 → 完成。
2. 打开软件 → 弹出“激活”窗口 → 复制窗口里的**机器码**发给你。
3. 收到你发来的**激活码**后，整段粘贴进输入框。
4. 点“激 活” → 进入软件。之后这台电脑就能一直用（买断、无到期）。
   - 这个激活码**只在这台电脑有效**；别人拿去别台电脑激活会失败。
5. 如果买家**换电脑/重装系统**导致指纹变了：让他把**新电脑的机器码**发你，你用 `--machine 新码` 再生成一个给他。

---
## 7 天免费试用（推荐，避免“激活后退款”漏洞）
离线一码一机**没办法远程吊销已激活的码**。所以别用“永久码+退款”，改用**会自动过期的试用码**：
```powershell
# 生成 7 天试用码（绑定买家机器码，7 天后自动锁）
python seller_tools/create_license.py --name "买家" --order "试用" --machine "买家机器码" --trial 7
```
- 程序到 7 天后会自动提示过期、要求重新激活——**不需要你锁**。
- 买家满意付款后，再发**永久码**（不带 `--trial`/`--exp`，即永久）替换试用码。
- **退款政策**就写成：“**7 天免费试用；正式激活前可全额退款**。”——因为试用码自动过期，不存在“退了还长期能用”。

### Shopee 售卖建议流程（先付款，7 天内可退款）
1. 买家在 **Shopee 下单付款**（RM 99）。
2. 你把「**下载链接 + 7 天试用码**（`--trial 7`，绑他的机器码）」发给他。
3. 他装好、激活、测试 7 天。
4. **7 天内退款** → 试用码自动过期，你没任何损失。
5. **7 天后没退** → 你发**永久码**（不带 `--trial/--exp`）给他，他重输即终生使用。

> 这样既满足“先付款后测试 + 7 天退款”，又**防住了“退了还白用”**。

---

## 你已完成的配置（不用再动）
- ✅ `seller_tools/private.pem`（你的私钥，已生成）
- ✅ `licensing/license_manager.py` 的 `PUBLIC_KEY_B64`（公钥，已填）
- ✅ `BIND_MODE = "pre"`（**一码一机**：激活码已绑定买家那台电脑，防转卖/共享）
- ✅ 本地自测通过：绑定本机 `(True, '激活成功')`；绑定他机被拒

---

## 注意
- `seller_tools/private.pem` 是**你的私钥**，已 gitignore，**别提交、别发给任何人**；弄丢就再也无法给老买家出码，**务必备份**。
- **每卖一份都必须用 `--machine "买家机器码"`**，否则生成的码没绑定电脑、任何人都能用（等于没防）。
- 买家换电脑/重装系统 → 机器指纹会变 → 旧的已激活记录失效。让他发新机器码，你再出**一个新码**给他（这也是你能控制“更换次数”的抓手）。
